interface:
  display_name: "Tester Role"
  short_description: "Verify behavior and produce quality evidence"
  default_prompt: "Use $tester-role to verify this implementation against its acceptance criteria."
policy:
  allow_implicit_invocation: true
